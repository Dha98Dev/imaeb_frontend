export interface paramsFilters{
    nivelId:number,
    nivelIds:number[]
    modalidadId?:number,
    modalidadIds:number[],
    sectorId:number,
    zonaId:number, 
    escuelaId:number,
    scope:string,
    sub:string
}